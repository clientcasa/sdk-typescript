/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type VenueAddress = {
  name: string | null;
  street: string | null;
  city: string | null;
  state: string | null;
  zip: string | null;
  country: string | null;
  lat: number | null;
  lng: number | null;
};

/** @internal */
export const VenueAddress$inboundSchema: z.ZodMiniType<VenueAddress, unknown> =
  z.object({
    name: types.nullable(types.string()),
    street: types.nullable(types.string()),
    city: types.nullable(types.string()),
    state: types.nullable(types.string()),
    zip: types.nullable(types.string()),
    country: types.nullable(types.string()),
    lat: types.nullable(types.number()),
    lng: types.nullable(types.number()),
  });

export function venueAddressFromJSON(
  jsonString: string,
): SafeParseResult<VenueAddress, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VenueAddress$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VenueAddress' from JSON`,
  );
}
