/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export const MilestoneParentType = {
  Proposals: "proposals",
  Projects: "projects",
} as const;
export type MilestoneParentType = OpenEnum<typeof MilestoneParentType>;

export const MilestoneStatus = {
  Pending: "pending",
  InProgress: "in-progress",
  Completed: "completed",
} as const;
export type MilestoneStatus = OpenEnum<typeof MilestoneStatus>;

export type Milestone = {
  /**
   * UUID v4
   */
  id: string;
  parentType: MilestoneParentType;
  /**
   * UUID v4
   */
  parentId: string;
  title: string;
  description: string | null;
  date: Date | null;
  time: string | null;
  status: MilestoneStatus;
  /**
   * ISO 8601 timestamp (UTC)
   */
  completedAt: Date | null;
  /**
   * UUID v4
   */
  assigneeId: string | null;
  /**
   * UUID v4
   */
  catalogItemId: string | null;
  sortOrder: number;
  /**
   * ISO 8601 timestamp (UTC)
   */
  createdAt: Date;
  /**
   * ISO 8601 timestamp (UTC)
   */
  updatedAt: Date;
};

/** @internal */
export const MilestoneParentType$inboundSchema: z.ZodMiniType<
  MilestoneParentType,
  unknown
> = openEnums.inboundSchema(MilestoneParentType);

/** @internal */
export const MilestoneStatus$inboundSchema: z.ZodMiniType<
  MilestoneStatus,
  unknown
> = openEnums.inboundSchema(MilestoneStatus);

/** @internal */
export const Milestone$inboundSchema: z.ZodMiniType<Milestone, unknown> = z
  .object({
    id: types.string(),
    parentType: MilestoneParentType$inboundSchema,
    parentId: types.string(),
    title: types.string(),
    description: types.nullable(types.string()),
    date: types.nullable(types.date()),
    time: types.nullable(types.string()),
    status: MilestoneStatus$inboundSchema,
    completedAt: types.nullable(types.date()),
    assigneeId: types.nullable(types.string()),
    catalogItemId: types.nullable(types.string()),
    sortOrder: types.number(),
    createdAt: types.date(),
    updatedAt: types.date(),
  });

export function milestoneFromJSON(
  jsonString: string,
): SafeParseResult<Milestone, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Milestone$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Milestone' from JSON`,
  );
}
